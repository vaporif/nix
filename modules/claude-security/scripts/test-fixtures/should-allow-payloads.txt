echo "hello"
echo 'world'
echo "---"
git commit -m "fix bug"
git commit -m "decompose UiPlugin into per-panel sub-plugins"
git log --oneline -5 && echo "---" && git status
git -C /tmp/repo add file.rs && git -C /tmp/repo commit -m "msg"
printf '%s\n' "line"
ls "some dir"
ls 'some dir'
echo a"b"c
echo a'b'c
echo $HOME
echo "$HOME"
cd $WORK_DIR && cargo test
find . -name \*.rs
find . -name "*.rs" -print
git diff "$(git merge-base HEAD main)"
RUST_LOG=debug cargo test
cargo test -- --nocapture
ls "$HOME/Documents"
