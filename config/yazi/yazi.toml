# Yazi configuration
# Video previews disabled due to ffmpeg CVEs (CVE-2023-51795 etc)

[preview]
max_width = 1000
max_height = 1000

[plugin]
prepend_previewers = [
  # Disable video previews - ffmpeg has unpatched CVEs
  { mime = "video/*", run = "empty" },
]
