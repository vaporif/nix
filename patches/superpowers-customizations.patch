diff -ruN superpowers-a/skills/brainstorming/SKILL.md superpowers-b/skills/brainstorming/SKILL.md
--- superpowers-a/skills/brainstorming/SKILL.md	2026-04-03 13:43:53
+++ superpowers-b/skills/brainstorming/SKILL.md	2026-04-03 13:44:21
@@ -22,20 +22,53 @@
 You MUST create a task for each of these items and complete them in order:
 
 1. **Explore project context** — check files, docs, recent commits
-2. **Offer visual companion** (if topic will involve visual questions) — this is its own message, not combined with a clarifying question. See the Visual Companion section below.
-3. **Ask clarifying questions** — one at a time, understand purpose/constraints/success criteria
-4. **Propose 2-3 approaches** — with trade-offs and your recommendation
-5. **Present design** — in sections scaled to their complexity, get user approval after each section
-6. **Write design doc** — save to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md` and commit
-7. **Spec self-review** — quick inline check for placeholders, contradictions, ambiguity, scope (see below)
-8. **User reviews written spec** — ask user to review the spec file before proceeding
-9. **Transition to implementation** — invoke writing-plans skill to create implementation plan
+2. **Check memory for prior context** — ask the user: "Want me to check memory for prior context on this?" If yes, run `/recall` with the topic. Do not auto-recall — past decisions may bias exploration.
+3. **Offer visual companion** (if topic will involve visual questions) — this is its own message, not combined with a clarifying question. See the Visual Companion section below.
+4. **Ask clarifying questions** — one at a time, understand purpose/constraints/success criteria
+5. **Propose 2-3 approaches** — with trade-offs and your recommendation
+6. **Present design** — in sections scaled to their complexity, get user approval after each section
+7. **Write design doc** — save to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
+8. **Spec review loop** — dispatch spec-document-reviewer subagent with precisely crafted review context (never your session history); fix issues and re-dispatch until approved (max 3 iterations, then surface to human)
+9. **User reviews written spec** — ask user to review the spec file before proceeding
+10. **Choose execution strategy** — ask the user which implementation approach to use:
+    - **Sequential** (default): invoke writing-plans → executing-plans (single agent, step by step)
+    - **Parallel subagents**: invoke writing-plans → subagent-driven-development (independent tasks in parallel, no inter-agent communication)
+    - **Agent teams**: invoke writing-plans, then spawn a coordinated team via `/team-feature` (persistent agents with tmux panes, shared task list, inter-agent messaging — best for large features with cross-cutting concerns)
+11. **Agent type assignments** (skip for sequential) — present an agent assignment table for each planned task. Analyze the design to suggest the best agent type per task based on language/domain. Present as a multi-select table for the user to review and override:
 
+    ```
+    Task                          | Suggested Agent                          | Override?
+    ------------------------------|------------------------------------------|----------
+    Implement auth middleware      | systems-programming:rust-pro             | [ ]
+    Add API endpoints              | systems-programming:golang-pro           | [ ]
+    Write Solidity contracts       | blockchain-web3:blockchain-developer     | [ ]
+    Update Nix config              | general-purpose                          | [ ]
+    ...                            | ...                                      | [ ]
+    ```
+
+    **Agent type selection guide:**
+
+    | Domain / Language      | Agent Type                               |
+    |------------------------|------------------------------------------|
+    | Rust                   | systems-programming:rust-pro             |
+    | Go                     | systems-programming:golang-pro           |
+    | C                      | systems-programming:c-pro                |
+    | C++                    | systems-programming:cpp-pro              |
+    | Solidity / Web3 / DeFi | blockchain-web3:blockchain-developer     |
+    | Security audit         | security-scanning:security-auditor       |
+    | Code review            | git-pr-workflows:code-reviewer           |
+    | General / other        | general-purpose (default)                |
+
+    Ask the user: "Here are my suggested agent assignments. Change any you'd like, or approve to continue." The user can override any row. Save the final assignments into the spec under an `## Agent Assignments` section so writing-plans and execution skills can reference them.
+12. **Transition to implementation** — invoke writing-plans skill to create the plan (include agent assignments), then execute with the chosen strategy
+
 ## Process Flow
 
 ```dot
 digraph brainstorming {
     "Explore project context" [shape=box];
+    "Check memory?" [shape=diamond];
+    "Run /recall with topic" [shape=box];
     "Visual questions ahead?" [shape=diamond];
     "Offer Visual Companion\n(own message, no other content)" [shape=box];
     "Ask clarifying questions" [shape=box];
@@ -43,11 +76,20 @@
     "Present design sections" [shape=box];
     "User approves design?" [shape=diamond];
     "Write design doc" [shape=box];
-    "Spec self-review\n(fix inline)" [shape=box];
+    "Spec review loop" [shape=box];
+    "Spec review passed?" [shape=diamond];
     "User reviews spec?" [shape=diamond];
-    "Invoke writing-plans skill" [shape=doublecircle];
+    "Choose execution strategy" [shape=diamond];
+    "Agent type assignments\n(multi-select table)" [shape=box];
+    "Sequential\n(executing-plans)" [shape=doublecircle];
+    "Parallel subagents\n(subagent-driven-dev)" [shape=doublecircle];
+    "Agent teams\n(/team-feature)" [shape=doublecircle];
+    "Invoke writing-plans skill" [shape=box];
 
-    "Explore project context" -> "Visual questions ahead?";
+    "Explore project context" -> "Check memory?";
+    "Check memory?" -> "Run /recall with topic" [label="user says yes"];
+    "Check memory?" -> "Visual questions ahead?" [label="no"];
+    "Run /recall with topic" -> "Visual questions ahead?";
     "Visual questions ahead?" -> "Offer Visual Companion\n(own message, no other content)" [label="yes"];
     "Visual questions ahead?" -> "Ask clarifying questions" [label="no"];
     "Offer Visual Companion\n(own message, no other content)" -> "Ask clarifying questions";
@@ -56,14 +98,22 @@
     "Present design sections" -> "User approves design?";
     "User approves design?" -> "Present design sections" [label="no, revise"];
     "User approves design?" -> "Write design doc" [label="yes"];
-    "Write design doc" -> "Spec self-review\n(fix inline)";
-    "Spec self-review\n(fix inline)" -> "User reviews spec?";
+    "Write design doc" -> "Spec review loop";
+    "Spec review loop" -> "Spec review passed?";
+    "Spec review passed?" -> "Spec review loop" [label="issues found,\nfix and re-dispatch"];
+    "Spec review passed?" -> "User reviews spec?" [label="approved"];
     "User reviews spec?" -> "Write design doc" [label="changes requested"];
-    "User reviews spec?" -> "Invoke writing-plans skill" [label="approved"];
+    "User reviews spec?" -> "Choose execution strategy" [label="approved"];
+    "Choose execution strategy" -> "Invoke writing-plans skill" [label="sequential"];
+    "Choose execution strategy" -> "Agent type assignments\n(multi-select table)" [label="parallel or teams"];
+    "Agent type assignments\n(multi-select table)" -> "Invoke writing-plans skill";
+    "Invoke writing-plans skill" -> "Sequential\n(executing-plans)" [label="sequential"];
+    "Invoke writing-plans skill" -> "Parallel subagents\n(subagent-driven-dev)" [label="parallel"];
+    "Invoke writing-plans skill" -> "Agent teams\n(/team-feature)" [label="teams"];
 }
 ```
 
-**The terminal state is invoking writing-plans.** Do NOT invoke frontend-design, mcp-builder, or any other implementation skill. The ONLY skill you invoke after brainstorming is writing-plans.
+**The terminal state is writing-plans followed by execution.** Do NOT invoke frontend-design, mcp-builder, or any other implementation skill. Always invoke writing-plans first, then execute with the user's chosen strategy.
 
 ## The Process
 
@@ -82,6 +132,7 @@
 - Propose 2-3 different approaches with trade-offs
 - Present options conversationally with your recommendation and reasoning
 - Lead with your recommended option and explain why
+- For each approach, list concrete pros and cons (see "Document Pros and Cons" section below)
 
 **Presenting the design:**
 
@@ -90,6 +141,7 @@
 - Ask after each section whether it looks right so far
 - Cover: architecture, components, data flow, error handling, testing
 - Be ready to go back and clarify if something doesn't make sense
+- Where the design makes a choice between alternatives, include the pros and cons that led to that choice
 
 **Design for isolation and clarity:**
 
@@ -111,29 +163,87 @@
 - Write the validated design (spec) to `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`
   - (User preferences for spec location override this default)
 - Use elements-of-style:writing-clearly-and-concisely skill if available
-- Commit the design document to git
 
-**Spec Self-Review:**
-After writing the spec document, look at it with fresh eyes:
+**Spec Review Loop:**
+After writing the spec document:
 
-1. **Placeholder scan:** Any "TBD", "TODO", incomplete sections, or vague requirements? Fix them.
-2. **Internal consistency:** Do any sections contradict each other? Does the architecture match the feature descriptions?
-3. **Scope check:** Is this focused enough for a single implementation plan, or does it need decomposition?
-4. **Ambiguity check:** Could any requirement be interpreted two different ways? If so, pick one and make it explicit.
+1. Dispatch `spec-document-reviewer` subagent using the template at `skills/brainstorming/spec-document-reviewer-prompt.md`
+2. Provide the subagent with the spec file path and precise review context — never your session history
+3. If issues are found, fix them and re-dispatch the reviewer
+4. Keep looping until 0 critical or medium issues remain
+5. If after 10 iterations there are still critical issues or a pattern of recurring medium issues, stop and ask the user for direction
 
-Fix any issues inline. No need to re-review — just fix and move on.
+Only proceed once the reviewer approves with zero critical/medium issues.
 
 **User Review Gate:**
 After the spec review loop passes, ask the user to review the written spec before proceeding:
 
-> "Spec written and committed to `<path>`. Please review it and let me know if you want to make any changes before we start writing out the implementation plan."
+> "Spec written and saved to `<path>`. Please review it and let me know if you want to make any changes before we start writing out the implementation plan."
 
 Wait for the user's response. If they request changes, make them and re-run the spec review loop. Only proceed once the user approves.
 
 **Implementation:**
 
 - Invoke the writing-plans skill to create a detailed implementation plan
-- Do NOT invoke any other skill. writing-plans is the next step.
+- Then execute with the user's chosen strategy:
+  - **Sequential**: invoke executing-plans (single agent works through tasks one by one)
+  - **Parallel subagents**: invoke subagent-driven-development (spawns independent Agent subprocesses for each task — no communication between them)
+  - **Agent teams**: use `/team-feature` to spawn a coordinated team (persistent agents in tmux panes with shared task list and inter-agent messaging — best for large features where agents need to coordinate). The team-lead MUST enforce two-stage review gates after each implementer completes a task:
+    1. **Spec compliance review**: team-reviewer reads actual code against task requirements — checks for missing requirements, extra/unneeded work, and misunderstandings. Does NOT trust the implementer's report. If issues found, implementer fixes and reviewer re-reviews until approved.
+    2. **Code quality review** (only after spec compliance passes): team-reviewer checks file responsibilities, decomposition, codebase pattern adherence, and standard quality concerns. Critical/Important issues block; Minor issues are noted. Implementer fixes and re-reviews until approved.
+    3. **Final holistic review**: after all tasks pass both stages, team-reviewer performs a cross-stream review checking consistency, integration seams, and cross-cutting concerns that per-task reviews might miss.
+    Never skip reviews. Never start code quality review before spec compliance passes. Never mark a task complete with open review issues.
+
+## Humanize All Non-Code Prose
+
+Apply the `humanizer` skill to ALL non-code text output throughout the entire brainstorming workflow. This includes:
+
+- **Design presentations** (approach proposals, design section walkthroughs)
+- **Spec documents** (the written design doc saved to disk)
+- **Plan prose** (goal descriptions, architecture summaries, task descriptions in plans)
+- **Questions and explanations** (clarifying questions, approach reasoning)
+
+**Does NOT apply to:** code blocks, file paths, commands, configuration snippets, or structured data (tables, checklists).
+
+**How to apply:** Before presenting any non-code prose to the user or writing it to a file, mentally run it through the humanizer patterns. Strip AI-isms inline as you write - don't produce slop then clean it up. Specifically watch for:
+- Significance inflation ("pivotal", "crucial", "vital role", "testament")
+- Promotional language ("vibrant", "groundbreaking", "seamless")
+- Copula avoidance ("serves as", "stands as" instead of "is")
+- Superficial -ing phrases ("highlighting", "underscoring", "fostering")
+- Rule-of-three lists, negative parallelisms ("not just X, but Y")
+- Filler ("in order to", "it is important to note")
+- Sycophantic openers ("Great question!", "Absolutely!")
+- Generic positive conclusions ("the future looks bright")
+- Em dash overuse, excessive boldface, emoji decoration
+- Inline-header vertical lists (prefer flowing prose)
+
+Write like a person talking to another person. Vary sentence rhythm. Have opinions. Be specific.
+
+## Document Pros and Cons for Every Decision
+
+Every design decision, architecture choice, library selection, or approach recommendation must include explicit pros and cons. This applies throughout the entire workflow:
+
+**During brainstorming:**
+- Each of the 2-3 proposed approaches needs a pros/cons list
+- When recommending one approach, explain which cons you're accepting and why
+
+**In spec documents:**
+- Architecture decisions get a "Pros / Cons / Why we chose this" block
+- Library or tool selections get the same treatment
+- If a decision has no meaningful cons, you probably haven't thought hard enough about it
+
+**In implementation plans:**
+- Task-level design choices (data structure selection, API shape, error handling strategy) include brief pros/cons
+- Keep it proportional: a minor choice gets a one-liner each, a major choice gets a full list
+
+**Format:** Keep it simple. No elaborate tables needed. Just:
+- Pros: what you gain
+- Cons: what you give up or risk
+- (Optional) Why: the tiebreaker reasoning if not obvious
+
+**Auto-select obvious winners:** If one option has only pros and no cons compared to the alternatives (or its cons are strictly a subset of every other option's cons), auto-select it. Still show all options with their pros/cons so the reasoning is visible, but announce: "Auto-selected option N — it has no downsides compared to the alternatives. Let me know if you'd prefer a different choice." If the user doesn't object, proceed with it.
+
+**The point is not ceremony.** It's making trade-offs visible so the user (and future readers of the spec/plan) can see what was considered and why.
 
 ## Key Principles
 
diff -ruN superpowers-a/skills/executing-plans/SKILL.md superpowers-b/skills/executing-plans/SKILL.md
--- superpowers-a/skills/executing-plans/SKILL.md	2026-04-03 13:43:53
+++ superpowers-b/skills/executing-plans/SKILL.md	2026-04-03 13:44:01
@@ -27,7 +27,12 @@
 1. Mark as in_progress
 2. Follow each step exactly (plan has bite-sized steps)
 3. Run verifications as specified
-4. Mark as completed
+4. After verifications pass, do a prose cleanup pass on changed files:
+   - Strip comments that restate what the code does
+   - Strip AI-obvious comments ("Ensures proper...", "Leverages...", "Facilitates...")
+   - Humanize remaining comments, docstrings, and error messages — direct and specific, no filler
+   - Keep comments that explain *why*, remove those that just say *what*
+5. Mark as completed
 
 ### Step 3: Complete Development
 
diff -ruN superpowers-a/skills/subagent-driven-development/SKILL.md superpowers-b/skills/subagent-driven-development/SKILL.md
--- superpowers-a/skills/subagent-driven-development/SKILL.md	2026-04-03 13:43:53
+++ superpowers-b/skills/subagent-driven-development/SKILL.md	2026-04-03 13:44:01
@@ -53,8 +53,9 @@
         "Spec reviewer subagent confirms code matches spec?" [shape=diamond];
         "Implementer subagent fixes spec gaps" [shape=box];
         "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" [shape=box];
-        "Code quality reviewer subagent approves?" [shape=diamond];
+        "0 critical/medium issues?" [shape=diamond];
         "Implementer subagent fixes quality issues" [shape=box];
+        "Prose cleanup pass\n(humanize comments, strip AI slop)" [shape=box];
         "Mark task complete in TodoWrite" [shape=box];
     }
 
@@ -73,10 +74,11 @@
     "Spec reviewer subagent confirms code matches spec?" -> "Implementer subagent fixes spec gaps" [label="no"];
     "Implementer subagent fixes spec gaps" -> "Dispatch spec reviewer subagent (./spec-reviewer-prompt.md)" [label="re-review"];
     "Spec reviewer subagent confirms code matches spec?" -> "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" [label="yes"];
-    "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" -> "Code quality reviewer subagent approves?";
-    "Code quality reviewer subagent approves?" -> "Implementer subagent fixes quality issues" [label="no"];
+    "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" -> "0 critical/medium issues?";
+    "0 critical/medium issues?" -> "Implementer subagent fixes quality issues" [label="no"];
     "Implementer subagent fixes quality issues" -> "Dispatch code quality reviewer subagent (./code-quality-reviewer-prompt.md)" [label="re-review"];
-    "Code quality reviewer subagent approves?" -> "Mark task complete in TodoWrite" [label="yes"];
+    "0 critical/medium issues?" -> "Prose cleanup pass\n(humanize comments, strip AI slop)" [label="yes"];
+    "Prose cleanup pass\n(humanize comments, strip AI slop)" -> "Mark task complete in TodoWrite";
     "Mark task complete in TodoWrite" -> "More tasks remain?";
     "More tasks remain?" -> "Dispatch implementer subagent (./implementer-prompt.md)" [label="yes"];
     "More tasks remain?" -> "Dispatch final code reviewer subagent for entire implementation" [label="no"];
@@ -99,6 +101,20 @@
 - Touches multiple files with integration concerns → standard model
 - Requires design judgment or broad codebase understanding → most capable model
 
+
+## Subagent Type Selection
+
+Choose the appropriate `subagent_type` for implementer agents based on the task's primary language or domain:
+
+| Domain / Language      | subagent_type                        |
+|------------------------|--------------------------------------|
+| Rust                   | systems-programming:rust-pro         |
+| Go                     | systems-programming:golang-pro       |
+| Solidity / Web3 / DeFi | blockchain-web3:blockchain-developer |
+| General / other        | general-purpose (default)            |
+
+For code quality reviews of Web3/Solidity code, use `git-pr-workflows:code-reviewer` and additionally dispatch `security-scanning:security-auditor` for a parallel security review.
+
 ## Handling Implementer Status
 
 Implementer subagents report one of four statuses. Handle each appropriately:
@@ -255,12 +271,33 @@
 **If reviewer finds issues:**
 - Implementer (same subagent) fixes them
 - Reviewer reviews again
-- Repeat until approved
+- Keep looping until 0 critical and 0 medium/important issues
+- If after 10 iterations there are still critical issues or recurring medium/important issues, stop and ask the user for direction
+- Minor issues are noted but don't block
 - Don't skip the re-review
 
 **If subagent fails task:**
 - Dispatch fix subagent with specific instructions
 - Don't try to fix manually (context pollution)
+
+## Prose Cleanup Pass
+
+After code quality review passes (0 critical/medium), run a prose cleanup pass on the task's changed files before marking it complete. This is a quick scan, not a full review.
+
+**What to fix:**
+- Strip comments that just restate what the code does ("// increment counter", "// return result")
+- Strip comments that smell like AI generated them ("// Ensures proper handling of...", "// Leverages the X pattern to facilitate...")
+- Rewrite remaining comments to sound human — direct, specific, no filler
+- Humanize docstrings: no "This function serves as...", no "facilitates", no "ensures". Just say what it does.
+- Humanize error messages: no "Failed to successfully...", no "Unable to properly..."
+- Humanize commit messages if they haven't been committed yet
+
+**What NOT to touch:**
+- Code logic, structure, tests
+- Comments that explain *why* (keep those, they're valuable)
+- License headers, generated code markers
+
+**How to dispatch:** Send the implementer subagent a focused cleanup prompt with the list of changed files. Keep it short — this is a 2-minute pass, not a rewrite.
 
 ## Integration
 
diff -ruN superpowers-a/skills/subagent-driven-development/code-quality-reviewer-prompt.md superpowers-b/skills/subagent-driven-development/code-quality-reviewer-prompt.md
--- superpowers-a/skills/subagent-driven-development/code-quality-reviewer-prompt.md	2026-04-03 13:43:53
+++ superpowers-b/skills/subagent-driven-development/code-quality-reviewer-prompt.md	2026-04-03 13:44:01
@@ -6,8 +6,10 @@
 
 **Only dispatch after spec compliance review passes.**
 
+**Reviewer selection:** For Web3/Solidity tasks, use `git-pr-workflows:code-reviewer` instead and additionally dispatch `security-scanning:security-auditor` for a parallel security review.
+
 ```
-Task tool (superpowers:code-reviewer):
+Task tool (superpowers:code-reviewer or git-pr-workflows:code-reviewer for Web3):
   Use template at requesting-code-review/code-reviewer.md
 
   WHAT_WAS_IMPLEMENTED: [from implementer's report]
@@ -24,3 +26,7 @@
 - Did this implementation create new files that are already large, or significantly grow existing files? (Don't flag pre-existing file sizes — focus on what this change contributed.)
 
 **Code reviewer returns:** Strengths, Issues (Critical/Important/Minor), Assessment
+
+**Review loop rules:** Keep reviewing and fixing until 0 Critical and 0 Important issues remain. Minor issues are noted but don't block approval. If after 10 iterations critical or recurring important issues persist, halt and ask the user for direction.
+
+**After approval:** dispatch a prose cleanup pass (see Prose Cleanup Pass in SKILL.md) to humanize comments, docstrings, and error messages, and strip AI-obvious comments before marking the task complete.
diff -ruN superpowers-a/skills/subagent-driven-development/implementer-prompt.md superpowers-b/skills/subagent-driven-development/implementer-prompt.md
--- superpowers-a/skills/subagent-driven-development/implementer-prompt.md	2026-04-03 13:43:53
+++ superpowers-b/skills/subagent-driven-development/implementer-prompt.md	2026-04-03 13:44:01
@@ -3,7 +3,7 @@
 Use this template when dispatching an implementer subagent.
 
 ```
-Task tool (general-purpose):
+Task tool ({subagent_type from Subagent Type Selection table}):
   description: "Implement Task N: [task name]"
   prompt: |
     You are implementing Task N: [task name]
@@ -70,6 +70,21 @@
     specifically what you're stuck on, what you've tried, and what kind of help you need.
     The controller can provide more context, re-dispatch with a more capable model,
     or break the task into smaller pieces.
+
+    ## Prose Quality
+
+    Any non-code text you write (comments, docstrings, error messages, documentation,
+    commit messages) must sound like a person wrote it. Avoid AI-isms: no "serves as",
+    "leverages", "ensures", "facilitates", no significance inflation ("crucial",
+    "pivotal"), no filler ("in order to", "it is important to note"), no sycophantic
+    tone. Be direct and specific.
+
+    ## Document Decision Trade-offs
+
+    When you make a choice not explicitly specified in the plan (picking between two
+    valid approaches, choosing a data structure, deciding on an error handling pattern),
+    note the pros and cons briefly in your report. If the decision is significant enough
+    to affect the design, flag it as DONE_WITH_CONCERNS so the controller can review it.
 
     ## Before Reporting Back: Self-Review
 
diff -ruN superpowers-a/skills/writing-plans/SKILL.md superpowers-b/skills/writing-plans/SKILL.md
--- superpowers-a/skills/writing-plans/SKILL.md	2026-04-03 13:43:53
+++ superpowers-b/skills/writing-plans/SKILL.md	2026-04-03 13:44:28
@@ -7,7 +7,7 @@
 
 ## Overview
 
-Write comprehensive implementation plans assuming the engineer has zero context for our codebase and questionable taste. Document everything they need to know: which files to touch for each task, code, testing, docs they might need to check, how to test it. Give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. Frequent commits.
+Write comprehensive implementation plans assuming the engineer has zero context for our codebase and questionable taste. Document everything they need to know: which files to touch for each task, code, testing, docs they might need to check, how to test it. Give them the whole plan as bite-sized tasks. DRY. YAGNI. TDD. KISS.
 
 Assume they are a skilled developer, but know almost nothing about our toolset or problem domain. Assume they don't know good test design very well.
 
@@ -40,7 +40,6 @@
 - "Run it to make sure it fails" - step
 - "Implement the minimal code to make the test pass" - step
 - "Run the tests and make sure they pass" - step
-- "Commit" - step
 
 ## Plan Document Header
 
@@ -95,12 +94,6 @@
 Run: `pytest tests/path/test.py::test_name -v`
 Expected: PASS
 
-- [ ] **Step 5: Commit**
-
-```bash
-git add tests/path/test.py src/path/file.py
-git commit -m "feat: add specific feature"
-```
 ````
 
 ## No Placeholders
@@ -113,11 +106,28 @@
 - Steps that describe what to do without showing how (code blocks required for code steps)
 - References to types, functions, or methods not defined in any task
 
+## Humanize Plan Prose
+
+Apply the `humanizer` skill to all non-code text in the plan document. The Goal, Architecture, task descriptions, and any explanatory prose must read like a person wrote them, not like an LLM assembled them from templates.
+
+**Applies to:** Goal statement, Architecture summary, task description text, explanatory notes, context sections.
+
+**Does NOT apply to:** code blocks, file paths, commands, expected output, step labels.
+
+Watch for and strip: significance inflation, promotional language, copula avoidance ("serves as" instead of "is"), superficial -ing phrases, filler, generic conclusions, rule-of-three padding. Be direct and specific.
+
+## Document Pros and Cons
+
+When the plan makes design choices (data structure, API shape, error strategy, library selection), include brief pros and cons inline. Keep it proportional to the decision's weight: a one-liner each for minor choices, a full list for major ones. The engineer reading this plan should understand what was considered and what trade-offs were accepted. If one option is strictly better (all pros, no cons vs alternatives), auto-select it and note: "Auto-selected — no downsides compared to alternatives. Let me know if you disagree."
+
 ## Remember
 - Exact file paths always
-- Complete code in every step — if a step changes code, show the code
+- Complete code in plan (not "add validation")
 - Exact commands with expected output
-- DRY, YAGNI, TDD, frequent commits
+- Reference relevant skills with @ syntax
+- DRY, YAGNI, TDD, KISS
+- Reuse existing utilities and dependencies before introducing new ones
+- Follow project security guidelines (CLAUDE.md, AGENTS.md) — validate inputs at system boundaries, review OWASP Top 10 and SWC Registry for relevant threat patterns
 
 ## Self-Review
 
